## 🎯 Your Turn: Create a Jellyfish Scene

### Goal
Practice using variables by creating your own jellyfish scene.

### Your Mission
Draw three jellyfish at different positions using variables!

### Steps
1. Create variables for positions: `x1`, `y1`, `x2`, `y2`, `x3`, `y3`
2. Create variables for sizes: `size1`, `size2`, `size3`
3. Use `ocean.jellyfish(x1, y1, size=size1)` to draw each one

### Starter Code Structure
```python
# Set up position variables
x1 = ???  # You fill in the numbers!
y1 = ???

# Draw using the variables
ocean.jellyfish(x1, y1, size=size1)
```

### Tips
- Jellyfish look good at sizes between 60 and 120
- Spread them across the canvas (0-800 for x, 0-600 for y)
- Try different colors: `OceanPalette.TRANSLUCENT_BLUE`, `OceanPalette.SHALLOW_WATER`

### Challenge
Add seaweed at the bottom using variables too! Use `ocean.seaweed(x, y, height=150)`
